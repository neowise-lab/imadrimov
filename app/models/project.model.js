export default class Project {

    constructor(id, title, description, thumb, photos) {
        this.id = id
        this.title = title
        this.description = description
        this.thumb = thumb
        this.photos = photos
    }
}
